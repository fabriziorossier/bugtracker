<div class="py-4">
    <h2>Lista de bugs asignados</h2>

    <hr>

    <table class="table table-dark">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Nombre</th>
                <th scope="col">Descripción</th>
                <th scope="col">Estado</th>
            </tr>
        </thead>
        <tbody>
            {{#each bugsByUser}}
                {{> bugByUser individualBug=this}}
            {{/each}}
        </tbody>
    </table>
</div>